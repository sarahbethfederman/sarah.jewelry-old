<html>
    <head>
        <title>sarah.jewelry</title>
        <link rel="stylesheet" href="css/main.css">
    </head>
    <body>
        <h1>{{title}}</h1>

        <nav>
            <ul>
                {{#each nav as |nav|}}
                    <li><a href="{{nav.url}}">{{nav.name}}</a></li>
                {{/each}}
                
                <li>cart</li>
            </ul>
        </nav>

        <main>
            <p>CONTENT</p>
        </main>

        {{#each scripts as |script|}}
            <script src="{{script}}"></script>
        {{/each}}
    </body>
</html>